```yml
name: XliffToPo

on:
  push:
    branches:
      - main
    paths:
      - 'uk/**/temp.xliff'

permissions:
  contents: write

jobs:
  merge_translations:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Display Python version
        run: python -c "import sys; print(sys.version)"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

      - name: Convert xliff to po
        run: |
          cat <<'EOF' > convert_translations.py
          import xml.etree.ElementTree as ET
          import re
          from pathlib import Path
          from datetime import datetime

          def xliff_to_po(xliff_file, po_file):
              tree = ET.parse(xliff_file)
              root = tree.getroot()

              ns = {'xliff': 'urn:oasis:names:tc:xliff:document:1.2'}

              translations = []

              for trans_unit in root.findall(".//xliff:trans-unit", ns):
                  source = trans_unit.find("xliff:source", ns)
                  target = trans_unit.find("xliff:target", ns)

                  source_text = source.text if source is not None and source.text else ""
                  target_text = target.text if target is not None and target.text else ""

                  source_text = source_text.replace('"', '\\"')
                  target_text = target_text.replace('"', '\\"')

                  source_text = source_text.replace('\n', '\\n')
                  target_text = target_text.replace('\n', '\\n')

                  context_group = trans_unit.find("xliff:context-group", ns)

                  if context_group is not None:
                      contexts = context_group.findall("xliff:context", ns)

                      for context in contexts:
                          if context.text:
                              lines = context.text.split("\n")

                              for line in lines:
                                  line = re.sub(r'&#13;', '', line)

                                  if line.startswith("#. "):
                                      msgctxt = line[3:].strip()
                                      translations.append(
                                          (msgctxt, source_text, target_text)
                                      )

              translations.sort(key=lambda x: x[0])

              revision_date = datetime.now().strftime("%Y-%m-%d %H:%M%z")

              with open(po_file, 'w', encoding='utf-8') as po:
                  po.write('msgid ""\n')
                  po.write('msgstr ""\n')
                  po.write('"Language: uk\\n"\n')
                  po.write('"Project-Id-Version: ULP Reborn\\n"\n')
                  po.write('"Language-Team: ULP Reborn Team\\n"\n')
                  po.write(f'"PO-Revision-Date: {revision_date}\\n"\n')
                  po.write('"Content-Type: text/plain; charset=utf-8\\n"\n')
                  po.write('"Content-Transfer-Encoding: 8bit\\n"\n')
                  po.write('"POT Version: 2.0\\n"\n\n')

                  for msgctxt, msgid, msgstr in translations:
                      po.write(f"#. {msgctxt}\n")
                      po.write(f'msgctxt "{msgctxt}"\n')
                      po.write(f'msgid "{msgid}"\n')
                      po.write(f'msgstr "{msgstr}"\n\n')

          xliff_files = list(Path("uk").rglob("temp.xliff"))

          if not xliff_files:
              raise Exception("No temp.xliff files found")

          for xliff_path in xliff_files:
              po_path = xliff_path.parent / "ukrainian.po"

              print(f"Converting: {xliff_path} -> {po_path}")

              xliff_to_po(
                  str(xliff_path),
                  str(po_path)
              )
          EOF

          python convert_translations.py

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add uk

          git diff --cached --quiet && echo "No changes to commit" && exit 0

          git commit -m "Convert xliff to po [skip ci]"

          git pull --rebase origin main

          git push origin HEAD:main
```
